function sumNoDuplicates(numList) {
  let result = [];
  let sum = 0;
  numList.forEach(num => {
    if (numList.indexOf(num) === numList.lastIndexOf(num)) {
      result.push(num);
      sum+=num;
    }
  });
  return sum;
}
